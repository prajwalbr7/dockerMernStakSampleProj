# Frontend (React + Vite)

Install and run:

```bash
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173` and use the contact form. The frontend sends a POST to `http://localhost:5000/submit`.
